# 풀이 기록

> 문제마다 네 줄을 남긴다. 정답 코드를 외우는 것보다 이 기록이 중요하다.
> `시간초과 → O(N²) → dict 사용 → O(N)` 처럼 패턴으로 축적되면 다음 문제에서 바로 꺼내 쓸 수 있다.
>
> 전략: [`storage/laboratory/CodingInterview/sk-hynix-2026-h2-plan.md`](../../storage/laboratory/CodingInterview/sk-hynix-2026-h2-plan.md)

---

## 반복해서 나온 패턴

| 증상 | 원인 | 해결 | 처음 겪은 문제 |
| --- | --- | --- | --- |
| 시간 초과 | 반복문 안에서 `.count()` / `.index()` / `리스트 in` → O(N²) | dict·set으로 조회를 O(1)로 | LV1 53 |
| ↑ 실측 (LV1 12) | 선수 50,000 / 호명 1,000,000 에서 `find` 로 매번 훑기 = **약 22초** | 해시로 위치 추적 → **108.7ms**. 200배 | LV1 12 |
| 큰 원소를 값으로 순회 | `for (string s : v)` 가 원소를 매번 복사 | **읽기만 하면 `const&`.** 실측 108.7ms → 71.8ms | LV1 12 |
| 동명이인·중복에서 오답 | `set`이 개수 정보를 버림 | 개수가 필요하면 dict, 종류만 필요하면 set | LV1 53 / 88 |
| 한 칸씩 어긋남 | 문제는 "n번째"(1부터), 파이썬은 `[0]`부터 | 시작은 `-1`, 슬라이스 끝은 그대로 | LV1 52 |
| 조용히 틀림 | 항상 참인 `if`, 도달 못 하는 기본값 반환 | 방어를 지우고 시끄럽게 터지게 둔다 | LV1 53 |
| **불필요한 방어 분기가 버그** | 결과식이 이미 경계를 처리하는데 조기 종료를 덧붙임 | 분기를 하나 지운다. 조건이 늘면 **틀릴 자리도 같이 는다** | LV1 81 |
| 마지막 덩어리·잔여분 누락 | 무언가를 "끝날 때" 세면 끝까지 안 끝난 마지막이 빠짐 | **세는 시점을 "시작"으로 옮긴다** → 경계 처리가 통째로 사라진다 | LV1 23 |
| 제한 100만인데 O(n²) 접근 | 제한 숫자를 안 읽고 "찾을 때까지 다시 훑기"부터 떠올림 | **제한 숫자로 허용 복잡도를 먼저 정하고** 접근을 고른다 | LV1 28 |
| 항상 거짓인 조건 | 같은 인덱스를 두 번 쓰고 하나를 빠뜨림 (`a[n-2]==3 && a[n-2]==2`) | **찾는 패턴과 같은 순서로** 인덱스를 적어 눈으로 대조 | LV1 28 |
| 그리디가 예제만 통과 | **순회 순서**를 정하지 않았거나 순서 없는 컨테이너를 씀 | 그리디는 **순서가 곧 정확성**이다. "이 순서가 왜 옳은가"에 답할 수 없으면 검증 안 된 것 | LV1 50 |
| 정수 나눗셈이 1 모자람 | `70/30 == 2` 인데 실제로는 3일 필요 (소수점 버림) | **올림 나눗셈 `(a + b - 1) / b`.** `ceil()` 은 double 오차 위험 | LV2 34 |
| 그리디를 근거 없이 씀 | 제한사항의 한 줄이 "선택이 갈리는가"를 결정하는데 그걸 안 읽음 | 제한사항마다 "여기서 고를 게 있나?" 를 묻는다. 없으면 그리디, 있으면 DP/백트래킹 | LV1 18 |
| 순회 중 리스트 변형 | `for x in lst:` 안에서 `lst.pop()` → 인덱스가 어긋남 | 원본을 건드리지 말고 "어디까지 썼는지" 포인터 변수로 기억 | LV1 18 |
| 범위 밖 접근 | 소진된 컨테이너의 `[i]` 를 그대로 읽음 | `and` / `&&` 단락 평가로 범위 검사를 **왼쪽에** 둔다 | LV1 18 |

---

## LV2

### 01 카카오프렌즈 컬러링북 (2026-09-15, Python·C++ 동시) — BFS 첫 문제

**혼자 풀었다. 힌트 없이 BFS 정석 구조를 그대로 짰다. 6/6 통과, 로직 수정 없음.**

- **이전 격자 문제와의 차이 — "얼마나 갈지 미리 모른다".**
  14번 공원 산책은 갈 칸 수가 명령에 적혀 있어 `for` 로 끝났다.
  여기서는 번지는 곳까지 번지므로 **"앞으로 가 볼 칸"을 큐에 쌓고 빌 때까지** 꺼낸다.
  이 차이가 "시뮬레이션 → 탐색"의 경계다.
- **visited 는 큐에 넣을 때 켠다.** 꺼낼 때 켜면 같은 칸이 여러 번 들어가 개수가 부풀고 큐가 커진다.
  처음부터 맞게 짰다.
- **바깥 이중 루프의 역할은 "새 영역의 시작점 찾기"뿐이다.**
  bfs 를 한 번 부르면 그 영역이 통째로 visited 되므로 **bfs 호출 횟수 = 영역 개수**.
  이 구조는 LV3 네트워크·섬 연결하기에서 그대로 재사용된다.
- ⚠️ **이 문제 고유 함정 — 채점기가 `solution` 을 한 프로세스에서 여러 번 부른다.**
  스켈레톤의 전역 변수를 그대로 쓰면 이전 호출 값이 남아 틀린다.
  지역 변수만 써서 애초에 안 걸렸다. **탐색 문제에서 visited 를 전역으로 두지 않는 습관.**

**고칠 점 2개 (정답에는 영향 없음, 습관 문제):**

- **`auto [r, c] = q.front();` 가 매개변수 `r, c` 를 가린다** (`-Wshadow` 경고).
  `color` 를 루프 전에 잡아 뒀기에 결과는 맞았지만, 시작 좌표를 루프 안에서 다시 써야 하는
  코드였다면 조용히 틀린다. → `cur_r, cur_c` 로 분리.
- **방향 배열을 `while` 안에서 만들고 있었다.** 칸마다 `vector`/리스트를 새로 할당한다.
  100×100 이면 1만 번. 지금 제한에선 측정해도 차이가 없지만 **함수 밖 상수로** 빼는 게 맞다.

복잡도: 모든 칸을 정확히 한 번씩만 방문하므로 O(m·n) = 10,000.
실측 100×100 한 덩어리(최악) Python **0.0028s**. 제한이 의미 없는 크기다.

### 43 더 맵게 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : 최소 힙에서 둘을 꺼내 섞고 다시 넣기. **흐름 도식을 먼저 그리고 시작**
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : **최소 힙** `priority_queue<long long, vector<long long>, greater<long long>>`. `O(n log n)`
- ④ 틀린 이유 : **두 번. 둘 다 타입 문제.**
  - 힙 원소를 `int` 로 둠 → `new = first + second*2` 가 30억까지 가는데 `int` 는 21억
  - **힙만 `long long` 으로 바꾸고 꺼내는 변수는 `int` 로 둠** → 여전히 오답

- **배운 것 1 — `priority_queue` 는 기본이 최대 힙이다.**
  Python `heapq` 가 기본 **최소** 힙인 것과 **정반대**다.
  최소 힙은 `priority_queue<T, vector<T>, greater<T>>` — `greater` 를 넘기려면 컨테이너 타입도 같이 적어야 한다.

- **배운 것 2 — 그릇이 아니라 "계산이 어느 타입에서 일어나는가"다.**
  `long long` 힙에 담아도 `int a = pq.top();` 로 꺼내 `int` 로 계산하면 **거기서 이미 넘친다.**
  힙은 결과를 보관할 뿐 계산을 대신해 주지 않는다. 59번(`vector<long long>` 에 담아도 `int*int`)과 같은 구조.
  실측: 1,000,000 짜리 원소 **128개 + K=10억** 에서 `int` 계산은 `-1`, 정답은 `127`.
  값이 섞을 때마다 약 3배씩 자라 `1e6 * 3^7 ≈ 21.9억` 에서 넘는다.
  **주어진 예제와 작은 랜덤 200,000건은 전부 통과한다** — 채점 전까지 안 드러난다.

- **배운 것 3 — `auto x = pq.top();` 이 이 사고를 구조적으로 막는다.**
  `auto` 는 참조와 `const` 를 떼고 값 타입으로 추론하므로 컨테이너 타입이 바뀌면 변수도 따라온다.
  단 **`auto&` 는 금지** — 힙 내부를 가리키는 참조라 `pop()` 뒤에는 죽은 자리를 읽는다.
  (35번의 `for (const auto& x : waiting)` 는 순회하며 읽기만 하므로 `&` 가 맞다.
   **"꺼낸 뒤 컨테이너를 건드리는가"** 가 기준이다.)

- `-1` 판정은 "힙이 빔"이 아니라 **"원소가 2개 미만"** 이다.
  섞을 때마다 둘이 나가고 하나가 들어와 크기가 1씩 줄므로 힙이 완전히 비는 경우는 없다.

---

### 32 다리를 지나는 트럭 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : 다리를 `bridge_length` 칸짜리 **컨베이어 벨트**(빈 칸 = 0)로 보고 1초씩 민다
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : `deque<int>` 두 개(다리, 대기 트럭) + 무게 합 변수. 최악 `O(시간)`
- ④ 틀린 이유 : **없음.** 한 번에 통과

- **배운 것 1 — 빈 칸을 0으로 채우면 "대수 제한"을 따로 셀 필요가 없다.**
  다리 길이 = 최대 동시 통행 대수이므로, 칸 수를 고정해두면 규칙이 저절로 지켜진다.
  무게 합은 매초 다시 더하지 않고 변수로 들고 다닌다 (안 그러면 `O(시간 × 다리길이)`).

- **배운 것 2 — 판정 순서.** 맨 앞 칸을 **내린 뒤**의 무게로 다음 트럭을 판정해야 한다.
  순서가 바뀌면 올릴 수 있는데 못 올리는 경우가 생긴다.

- **배운 것 3 — "복잡해진다"는 느낌은 멈추라는 신호다.**
  최악(다리 10,000 / 트럭 10,000대 × 최대무게)에서 루프가 **1억 번** 돈다.
  C++ 실측 **249.8ms** — 통과한다. Python 은 약 4.7초로 시간 초과.
  Python 에 "시간 점프"를 넣어 봤더니 **정확했지만 2.3배뿐**이었고(0.185s → 0.079s)
  `sum(1 for x in bridge if x != 0)` 가 매초 다리를 훑어 이득을 도로 까먹었다. **여전히 TLE.**
  → 복잡도를 올렸는데 **통과 여부가 안 바뀌면 순손실**이다.
  → 점프를 제대로 하려면 표현을 `(무게, 나갈 시각)` 으로 바꿔야 하는데, 그쪽은 경계가 까다롭다
     (Claude 가 실시간으로 두 번 틀렸다). **컨베이어 벨트가 이 문제에는 맞는 표현이다.**

- **판단 기준:** "이 최적화가 통과 여부를 바꾸는가" → 아니면 거기서 멈춘다.
  시험장에서 이미 통과할 코드를 빠르게 만드는 데 20분 쓰면 한 문제를 잃는다.

---

### 35 프로세스 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : 덱에 `(우선순위, 원래 인덱스)` 를 담고, 맨 앞이 최고가 아니면 맨 뒤로 보내기
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : `deque<pair<int,int>>`. 앞에서 빼고 뒤에 넣는 것이 둘 다 `O(1)`
- ④ 틀린 이유 : **없음.** 분석 먼저 한 여섯 번째 문제 (50 · 32 · 05 · 34 · 82 · 35)

- **배운 것 1 — "다시 꺼내 볼 일이 있는가" 기준이 실제로 작동했다.**
  34번 기능개발: 한 번 지나간 작업을 다시 볼 일이 없다 → **큐 불필요**, 변수 두 개로 끝.
  35번 프로세스: 맨 뒤로 보낸 것을 **나중에 다시 꺼낸다** → **큐가 진짜로 필요**.
  같은 "스택/큐" 폴더의 두 문제가 정반대다. **폴더 이름이 아니라 이 질문이 자료구조를 정한다.**

- **배운 것 2 — 인덱스를 함께 담아야 하는 경우.**
  우선순위만 담으면 `[1,1,9,1,1,1]` 처럼 같은 값이 여럿일 때 어느 것이 `location` 인지 구분할 수 없다.
  82번에서는 정렬 후 인덱스를 쓸 일이 없어 낭비였는데, 여기서는 **인덱스가 문제의 핵심**이다.
  → **"정렬·이동 후에도 원래 위치를 답에 써야 하는가"** 가 기준이다.

- **배운 것 3 — 상태 전이가 있는 문제는 주석 맨 위에 흐름 도식을 그린다.** (사용자 제안, 세션 가이드 ⑧에 반영)

- 정리한 것: 매 반복마다 임시 `vector` 를 만들어 `max_element` 를 부르던 것을
  덱을 직접 훑는 방식으로 교체. `const auto&` 로 받아 원소 복사도 제거.
  오타 `new_priorites`(t 누락)는 일관되게 쓰여 **컴파일이 됐다** — 컴파일러가 못 잡는 종류.

---

### 34 기능개발 (2026-09-11, C++ 우선)
- ① 처음 떠올린 방법 : **하루씩 시뮬레이션.** 매일 전원 진도를 올리고 앞에서부터 완성된 것을 세어 제거
- ② 실제 정답 방법 : 각 작업의 **소요 일수를 먼저 계산**하고, 일수 배열을 그룹으로 나눈다
- ③ 사용한 자료구조 : 없음. 변수 두 개(현재 그룹의 배포일, 개수). `O(n)`
- ④ 틀린 이유 : **없음.** 두 방식 다 한 번에 통과. 분석 먼저 한 네 번째 문제 (50 · 32 · 05 · 34)

- **배운 것 1 — 올림 나눗셈 `(a + b - 1) / b`.**
  정수 나눗셈은 소수점을 버린다. 진도 30%, 하루 30 이면 `70/30 == 2` 지만 실제로는 **3일째**에 100을 넘는다.
  `(70 + 29) / 30 == 3`. `ceil()` 은 double 을 거쳐 오차 위험이 있어 정수 문제에서는 이 관용구를 쓴다.
  **하루씩 시뮬레이션하면 나눗셈 자체가 없어 이 함정을 우회할 수 있다** — 실제로 첫 풀이는 그렇게 피해 갔다.

- **배운 것 2 — "스택/큐 유형"이라고 큐를 쓰는 게 아니다.**
  이 문제는 **한 번 지나간 작업을 다시 볼 일이 없어 쌓아둘 것이 없다.** 변수 두 개면 끝난다.
  대비: 28번 햄버거는 4개를 빼낸 뒤 **그 아래가 다시 검사 대상**이 되므로 스택이 진짜로 필요했다.
  → **"다시 꺼내 볼 일이 있는가"** 가 스택·큐를 쓸지 가르는 기준이다.

- **배운 것 3 — 그룹 확정의 마지막 하나.**
  "새 그룹이 시작될 때 직전 그룹을 push" 하는 구조라 **마지막 그룹이 루프 안에서 확정되지 않는다.**
  23번 문자열 나누기에서 겪은 자리와 동일하며, 여기서는 루프 뒤 한 줄로 처리했다.

- 복잡도 비교(100개, 진도 1, 속도 1 → 99일, 20000회 평균):
  하루씩 시뮬레이션 `O(D·n)` **0.0022ms** vs 일수 계산 `O(n)` **0.00018ms** (12배).
  제한이 `n <= 100` 이라 **둘 다 통과한다.** 시뮬레이션의 `erase(begin(), begin()+c)` 는
  남은 원소를 전부 앞으로 당기므로 큰 입력에서는 그것만으로 `O(n^2)` 이 된다.

---

### 05 행렬의 곱셈 (2026-09-11, C++ 우선)
- ① 처음 떠올린 방법 : 삼중 루프. row / mid / col 세 크기에 이름을 붙이고 시작
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : 2차원 `vector`. `O(row·col·mid)` = 최대 100만, 실측 0.31ms
- ④ 틀린 이유 : **없음.** 분석 먼저 한 세 번째 문제, 세 번 다 한 번에 통과 (50 · 32 · 05)

- **배운 것 — 이 문제의 난관은 알고리즘이 아니라 "인덱스가 무엇을 가리키는가"다.**
  `i`(결과의 행) · `j`(결과의 열) · `k`(짝짓기 위치) 세 개가 뒤섞이기 쉽다.
  `row = arr1.size()`, `mid = arr2.size()`, `col = arr2[0].size()` 처럼 **크기에 이름을 붙여 두면**
  루프 세 개의 범위가 저절로 정해진다. `k` 가 arr1 에서는 "열", arr2 에서는 "행" 인 것이 곱의 정의 그 자체다.

- 값 범위 점검: 원소 -10~20, `mid` 최대 100 → 합의 절댓값이 최대 `20 × 20 × 100 = 40,000`.
  `int` 로 충분하다. (59번처럼 제한을 곱해보고 타입을 정하는 습관)

- `mid` 는 `arr1[0].size()` 로 잡아도 같다. **"곱할 수 있다"는 것이 곧 그 둘이 같다는 뜻**이고,
  제한사항이 그것을 보장한다.

---

## LV1

### 14 공원 산책 (2026-09-14, Python·C++ 동시) — 격자 시뮬레이션

**혼자 풀었다. 힌트는 "확인 후 이동" 한 줄뿐이었고 그 구조를 그대로 구현했다. 6/6 통과, 고칠 곳 없음.**

- **핵심은 "도착지가 아니라 지나는 칸 전부"다.** 예제 3이 그것만 노린다 — 도착지는 `O` 인데
  가는 길에 `X` 가 있다. 도착지만 보면 예제 2까지는 통과하고 3에서 처음 틀린다.
- **쓴 구조 — 확인 후 커밋.** 임시 좌표 `nr, nc` 로 끝까지 걸어 보고, `valid` 가 살아남았을 때에만
  `row, col` 에 반영한다. 걸으면서 바로 `row, col` 을 고치면 중간에 막혔을 때 되돌릴 수 없다.
  **"전부 되거나 전부 안 되거나"인 연산은 원본을 마지막에 한 번만 건드린다.**
- **범위 검사가 격자 접근보다 먼저.** Python 은 음수 인덱스가 뒤에서부터 읽히므로
  에러조차 나지 않고 **조용히 틀린다.** 10번에서 배운 것과 같은 함정.
- **`park` 은 정사각형이 아니다** (세로 n, 가로 m 각각 3~50). `park.size()` 하나로 두 방향을
  검사하면 틀린다. 10번은 정사각형이라 이 차이가 안 드러났다.
- 명령 파싱: `route.split(" ")` ↔ `stringstream ss(route); ss >> command >> number;`
  길이가 `"E 2"` 로 고정이라 `route[0]`, `route[2]-'0'` 도 되지만, stringstream 이 두 자리 수에도
  견디고 읽기도 낫다. **코테에서는 둘 다 정답.**

복잡도: 시작점 찾기 O(n·m) + 명령 처리 O(R·k) = 2500 + 450. 제한이 아무 의미 없는 크기다.

### 10 이웃한 칸 (2026-09-13, C++ 우선) — 격자 탐색 입문
- ① 처음 떠올린 방법 : 방향 배열로 상하좌우를 훑고 범위 검사 후 색 비교
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : 방향 배열 두 개. `O(4)`
- ④ 틀린 이유 : **없음**

- **배운 것 — 격자 문제의 두 습관.** BFS/DFS 로 가기 전에 이 둘부터 손에 붙인다.

  ```cpp
  int dr[4] = {-1, 1, 0, 0};    // 상 하 좌 우. 순서를 항상 같게 유지한다
  int dc[4] = { 0, 0,-1, 1};

  if (nr < 0 || nr >= R || nc < 0 || nc >= C) continue;   // 범위 먼저
  if (board[nr][nc] == ...) ...                           // board 는 그다음
  ```

  **범위 검사를 `board` 접근보다 앞에 두는 것이 핵심.** 순서가 바뀌면 격자 밖 메모리를 읽고
  **에러 없이 이상한 값**이 나온다. 모서리 칸에서 바로 걸린다.
  **Python 은 더 고약하다** — 음수 인덱스가 뒤에서 세는 것이라 조용히 **엉뚱한 칸**을 읽는다.

- 방향 배열은 크기가 4로 고정이므로 `vector` 대신 일반 배열로 충분하다 (32번과 같은 판단).

### 25 기사단원의 무기 (2026-09-13, C++ 우선)
- ① 처음 떠올린 방법 : 각 수마다 `sqrt` 까지 나눠 약수 개수를 세고 `limit` 초과면 `power` 로 대체
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : 없음. `O(n sqrt(n))` = 100,000 x 316 약 3,160만
- ④ 틀린 이유 : **없음**

- **배운 것 — 약수는 쌍으로 나온다.**
  `i = d * (i/d)` 이므로 둘 중 작은 쪽은 반드시 `sqrt(i)` 이하다.
  따라서 `d*d <= i` 까지만 훑으며 약수를 찾으면 `d` 와 `i/d` **두 개**를 센다.
  단 `d*d == i` (완전제곱수)이면 짝이 자기 자신이라 **하나만** 센다. 이 분기를 빼면 4, 9, 16이 틀린다.
  57번 소수 판정의 `sqrt` 와 같은 원리이고, **약수 개수 세기의 표준 형태**다.

- **더 빠른 방법을 알지만 쓰지 않았다.**
  `for d: for m = d; m <= n; m += d` 로 **"각 수의 약수를 찾는다"를 "각 수의 배수를 훑는다"로 뒤집으면**
  `n/1 + n/2 + ... = O(n log n)` 이 된다 (에라토스테네스의 체와 같은 발상).
  실측(number=100,000): `sqrt` 방식 **25.1ms** vs 배수 순회 **0.4ms**, 60배.
  그러나 25ms 로 이미 통과하므로 쓰지 않는다. **"이 최적화가 통과 여부를 바꾸는가"** 가 아니면 멈춘다.
  (58번 별찍기·32번 다리를 지나는 트럭에서 세운 판단과 같다)

### 38 부족한 금액 계산하기 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : `long long remain = money` 에서 `price*c` 를 차례로 빼기
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : 없음. `O(count)` = 최대 2,500회
- ④ 틀린 이유 : **없음.** 오버플로를 구조적으로 피했다

- **배운 것 — 누적 변수만 큰 타입이면 되는 경우가 있다.**
  한 항 `price * c` 는 최대 `2500 * 2500 = 6,250,000` 으로 `int` 안에서 안전하고,
  커지는 것은 **누적값(최대 78억)뿐**이다. 그래서 `remain` 만 `long long` 이면 된다.
  반면 공식 `price * count * (count+1) / 2` 를 `int` 로 쓰면 **곱하는 순간 넘친다**
  (실측: 최대치 `(2500, 1, 2500)` 에서 `0` 이 나온다. 정답은 `7,815,624,999`).
  → 43번의 "그릇이 아니라 계산 위치"와 같은 이야기. **어디서 커지는지를 보면 어디를 키울지가 나온다.**

- 루프가 최대 2,500번이라 성능도 문제없다. **공식이 항상 나은 것은 아니다** — 여기서는 루프가 더 안전했다.

### 57 소수 만들기 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : 삼중 루프로 3개 조합을 만들고 합이 소수인지 판정
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : 없음. `O(C(n,3) x sqrt(합))` = 19,600 x 55 정도
- ④ 틀린 이유 : **없음.** 한 번에 통과

- **배운 것 — 소수 판정은 `sqrt` 까지만 본다. 그리고 `+1` 이 핵심이다.**
  `total = a * b` 라면 둘 중 하나는 반드시 `sqrt(total)` 이하다.
  따라서 `sqrt` 까지 훑어 약수가 없으면 그 위에도 없다. `O(total)` → `O(sqrt(total))`.
  **`int limit = sqrt(total) + 1;` 의 `+1` 이 없으면 완전제곱수(9, 25, 49 …)를 소수로 오판한다.**
  제곱근 자신을 검사 범위에 넣어야 하기 때문이다.
  (합의 범위 6~3000 전체를 `2..total-1` 무식한 판정과 대조해 불일치 0 확인)

- 조합을 만들 때 `j = i+1`, `k = j+1` 로 시작하면 같은 조합을 순서만 바꿔 다시 세지 않는다.
  46번 두 개 뽑아서 더하기에서 `j` 시작점을 빠뜨려 틀렸던 것과 같은 자리다.

- **정수론 입구.** 2026 상반기 2번이 G3 정수론이었으므로 이 도구는 반드시 필요하다.

### 48 크레인 인형뽑기 게임 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : 열마다 스택을 미리 만들고, 바구니도 스택으로 두어 연속 중복을 터뜨림
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : 열별 `vector<int>` 스택 + 바구니 스택. `O(N² + moves)`
- ④ 틀린 이유 : **없음.** 한 번에 통과

- **배운 것 1 — 28번 햄버거와 같은 구조다.**
  바구니에서 둘이 터지면 **그 아래가 새 맨 위**가 되어 다음 인형과 다시 비교된다.
  "다시 꺼내 볼 일이 있다" → 스택. 34번 기능개발(다시 볼 일 없음 → 변수 두 개)과 대비된다.

- **배운 것 2 — 전처리로 반복 탐색을 없앤다.**
  매번 격자를 위에서부터 훑는 대신 열별 스택을 한 번만 만들어 둔다.
  **아래 행부터 `push_back`** 하면 `back()` 이 곧 "가장 위 인형"이 된다.

- **배운 것 3 — 참조를 쓸지 말지는 "참조 대상이 사라지는가"로 정한다.**
  여기서 `vector<int>& tmp_stack = stacks[move-1];` 은 **맞다** — 컨테이너 자체를 고치려는 것이다.
  43번의 `auto& x = pq.top();` 은 **틀리다** — `pop()` 하면 그 원소가 사라진다.
  35번의 `for (const auto& x : waiting)` 은 맞다 — 순회하며 읽기만 한다.

- Python 쪽에서 `stacks = [[] for _ in range(rows)]` 로 썼다가 고쳤다.
  스택 개수는 **열** 개수인데 행 개수로 만든 것. `N×N` 격자라 **우연히 동작**했고,
  직사각형이었다면 바로 터졌을 자리다. **같은 코드를 두 언어로 옮길 때 한쪽에만 생기는 실수.**

### 12 달리기 경주 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : 이름 → 현재 위치 해시를 만들고, 호명될 때마다 swap + 위치표 갱신
- ② 실제 정답 방법 : 같음
- ③ 사용한 자료구조 : `unordered_map<string,int>` (이름 → 인덱스). `O(호명 수)`
- ④ 틀린 이유 : **없음.** 제한(호명 100만)을 보고 해시를 먼저 고른 것이 주효했다

- **배운 것 1 — 제한 숫자가 자료구조를 정한다.**
  호명 100만 × 선수 5만 = 500억. **실측 약 22초** (호명 수를 2배로 늘릴 때마다 시간도 2배 = `O(n·m)`).
  해시로 위치를 추적하면 **108.7ms**. **200배** 차이다.

- **배운 것 2 — 자리를 바꾸면 위치표도 같이 고쳐야 한다.**
  이 문제에서 가장 틀리기 쉬운 자리다. `swap` 이 끝난 뒤이므로
  `players[i-1]` 이 방금 호명된 선수, `players[i]` 가 밀려난 선수다. **읽는 순서가 하나만 어긋나도 틀린다.**

- **배운 것 3 — `const&` 로 순회하는 것이 실제 비용 차이를 만든다.**
  `for (string c : callings)` 는 문자열을 100만 번 복사한다. 실측 108.7ms → **71.8ms**.
  원소가 `int` 면 무의미하지만 `string`·`vector` 면 다르다. **읽기만 하면 `const&`.**

- `1등은 불리지 않는다`는 제한 덕분에 `i-1 < 0` 방어가 필요 없다.
  (제한이 보장하는 것은 방어하지 않는다 — 78번과 같은 판단)

### 82 문자열 내 마음대로 정렬하기 (2026-09-12, C++ 우선)
- ① 처음 떠올린 방법 : `(인덱스, 문자열)` 쌍을 만들어 정렬한 뒤 인덱스로 원본에서 다시 꺼냄
- ② 실제 정답 방법 : 인덱스 없이 `strings` 를 제자리에서 정렬해 그대로 반환
- ③ 사용한 자료구조 : 없음. `sort` + 람다 비교 함수. `O(n log n · 문자열 길이)`
- ④ 틀린 이유 : **없음.** 비교 함수를 처음부터 맞게 썼다. 다만 인덱스 왕복이 불필요했다.

- **배운 것 1 — C++ `sort` 의 비교 함수는 key 가 아니라 "순서"를 답한다.**
  Python `sorted(key=...)` 는 **무엇으로 비교할지**를 주고, C++은 **"a 가 b 보다 먼저인가"** 에 직접 답한다.
  다중 기준은 `if (1순위가 다르면) return 1순위 비교; return 2순위 비교;` 로 이어 붙인다.
  이 모양이면 기준이 3개 이상으로 늘어도 아래에 그대로 추가된다.
  Python 의 `key=lambda x: (x[n], x)` 튜플이 하는 일과 정확히 같다.

- **배운 것 2 — 비교 함수는 "같으면 반드시 `false`" 여야 한다.**
  `<=` 를 쓰면 같은 원소에 대해 "a 가 b 보다 앞이면서 b 도 a 보다 앞"이라는 모순이 되어
  `sort` 가 **런타임에 터지거나 무한 루프**에 빠진다. C++ 초보가 가장 자주 겪는 사고 중 하나다.

- **배운 것 3 — 인덱스를 들고 다닐 이유가 있는지 확인한다.**
  정렬 후 **"원래 몇 번째였는지"를 답에 써야 할 때만** `(인덱스, 값)` 쌍이 필요하다.
  값 자체를 순서대로 돌려주는 문제면 인덱스는 왕복만 하고 사라진다.

- 매개변수가 값 복사이므로 제자리 정렬 후 그대로 반환했다.
  72번(문자열 제자리 수정) · 28번(배열을 스택으로 재사용)에 이어 **세 번째**로 같은 원리다.

### 32 숫자 짝꿍 — C++ 재구현 (2026-09-10)
- ① 처음 떠올린 방법 : **분석부터 했다.** 제한 300만을 보고 정렬을 배제, 숫자가 0~9 열 개뿐이라 카운팅으로 결정
- ② 실제 정답 방법 : 같음. 자릿수별 등장 횟수를 세고 `min` 이 공통 개수, 9부터 내려오며 이어붙임
- ③ 사용한 자료구조 : 길이 10짜리 카운팅 배열 두 개. `O(n)` 시간 / `O(1)` 공간
- ④ 틀린 이유 : **없음.** 50번에 이어 분석 먼저 한 두 번째 문제, 두 번 다 한 번에 통과.

- **배운 것 1 — 값의 종류가 적으면 정렬 대신 카운팅이다.**
  숫자는 0~9 열 가지뿐이라 "몇 번 나왔나"만 세면 원본 순서는 필요 없다.
  정렬은 `O(n log n)` 이지만 카운팅은 `O(n)` 이고, 300만에서 이 차이가 결정적이다.
  → **제한이 크고 값의 가짓수가 작으면 카운팅 배열을 먼저 떠올린다.**

- **배운 것 2 — 특수 케이스는 마지막에 한곳에서 처리한다.**
  "공통 없음 → -1", "0들뿐 → 0" 두 가지를 루프 안에서 분기하지 않고 결과를 만든 뒤에 판정했다.
  9부터 내려왔다는 성질 덕분에 **첫 글자가 '0' 이면 전부 '0'** 이라 검사가 한 줄로 끝난다.
  (81번처럼 루프 안에 방어를 넣었으면 버그의 자리가 늘었을 것이다)

- **정리한 것** : 카운팅 배열을 `push_back` 20줄 대신 `int v[10] = {0};` 한 줄로,
  중간 `vector<string>` 을 없애고 `result` 에 직접, `to_string` 반복 대신 `append(cnt, char('0'+d))`.
  실측(각 300만자): 5.6ms → **2.0ms**. 둘 다 통과하므로 성능이 목적은 아니고,
  `append(n, c)` / `string(n, c)` 가 이날만 세 번째 나온 패턴이라 손에 붙이는 것이 목적이다 (58 · 76 · 32).

### 50 체육복 — C++ 재구현 (2026-09-09)
- ① 처음 떠올린 방법 : **문제를 먼저 분석**했다. 제한사항의 "여벌을 가져왔는데 도난당한 학생" 줄을 읽고
  코드 쓰기 전에 "교집합을 먼저 걷어내야 한다"와 "그리디 문제다"를 잡아냄
- ② 실제 정답 방법 : 교집합을 양쪽에서 제거 → 낮은 번호 giver 부터 **왼쪽(r-1) 우선**으로 빌려주기
- ③ 사용한 자료구조 : `set<int>` 두 개. 조회·삭제가 `O(log n)` 이고 **순회가 오름차순**이라 정렬이 공짜
- ④ 틀린 이유 : **없음.** 분석 단계를 먼저 거친 첫 문제였고 한 번에 통과했다.

- **배운 것 1 — 그리디의 정당성은 교환 논증으로 증명한다.**
  남은 giver 중 가장 작은 `r` 에 대해, `r-1` 을 도울 수 있는 사람은 `r-2` 와 `r` 뿐인데
  `r` 이 가장 작은 giver 이므로 `r-2` 는 giver 가 아니다 → **`r-1` 은 `r` 말고 대안이 없다.**
  반면 `r+1` 은 `r+2` 가 도울 수도 있다. **대안이 없는 쪽부터 주는 것이 절대 손해가 아니다.**
  실측 반례: 오른쪽 우선으로 바꾸면 `n=4, lost=[1,3], reserve=[2,4]` 가 4 대신 3.
  랜덤 150,026건 중 **1,093건**에서 오른쪽 우선이 손해.

- **배운 것 2 — 그리디는 "순회 순서"가 곧 정확성이다. 그리고 그 순서를 컨테이너가 떠받친다.**
  이 코드에는 `sort` 가 없다. **C++ `set` 이 순회 시 오름차순을 보장**하기 때문이다(46번에서 배운 성질).
  로직을 한 글자도 안 고치고 **`unordered_set` 으로만 바꾸면 랜덤 15만 건 중 1,078건이 틀린다.**
  Python 쪽에서 `sorted(reserve)` 를 명시한 것과 같은 요구다.
  → **그리디를 쓸 때는 항상 "이 순서가 왜 옳은가"에 답할 수 있어야 한다.**

- **배운 것 3 — 그리디는 작은 입력에서 완전탐색과 대조해 검증한다.**
  n 을 2~10 으로 줄이고 모든 빌려주기 조합(왼쪽/오른쪽/안 줌)을 완전탐색한 최적값과 비교했다.
  **225,490건 불일치 0.** "맞는 것 같다"를 "맞다"로 바꾸는 데 5분이면 된다.

- **배운 것 4 — 코드보다 분석이 먼저다.**
  제한사항을 문장 단위로 읽으며 각 줄이 알고리즘의 무엇을 결정하는지 먼저 적었더니 한 번에 통과했다.
  전날 28번은 제한(100만)을 안 읽고 재스캔부터 떠올려 네 번 틀렸다. **같은 사람, 다른 순서, 다른 결과.**

### 28 햄버거 만들기 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : 배열 전체를 훑어 `1,2,3,1` 을 찾고 지우기를 **더 없을 때까지 반복** (재스캔)
- ② 실제 정답 방법 : 스택에 하나씩 쌓고 **쌓을 때마다 맨 위 4개만** 검사. 맞으면 4개 빼기
- ③ 사용한 자료구조 : 스택. 단 별도 컨테이너 없이 **입력 배열을 스택으로 재사용** + 정수 `top` 하나
  → `O(n)` 시간 / 추가 공간 `O(1)`
- ④ 틀린 이유 : **네 번.**
  - `while True` 에 `break` 가 없어 무한 루프. `return` 에 도달 못 함
  - 재스캔 자체가 `O(n²)` → 제한 1,000,000 에서 10¹² 번. **접근이 틀렸다**
  - **검사 순서를 뒤집음.** 재료를 넣기 *전에* 검사하고 `continue` 로 그 재료를 버렸다.
    햄버거는 재료를 하나 더 얹었기 때문에 완성되므로 **얹고 나서** 봐야 한다
  - `a[n-2]==3 && a[n-2]==2` — 같은 인덱스를 두 번 쓰고 `n-4` 를 빠뜨림.
    **항상 거짓**이라 전부 0을 반환. 문법은 정상이라 **컴파일러가 못 잡는다**

- **배운 것 1 — 제한 숫자를 보고 허용 복잡도를 먼저 정한다.**

  | 제한 n | 허용 복잡도 |
  | --- | --- |
  | ~1,000 | `O(n²)` 가능 |
  | ~100,000 | `O(n log n)` 까지 |
  | ~1,000,000 | **`O(n)` 또는 `O(n log n)`** |

  재스캔 실측(앞에 매칭 안 되는 재료를 길게 깐 최악 케이스): 길이 4,000 → 0.0103s,
  8,000 → 0.0416s, 16,000 → 0.1356s, 32,000 → 0.5465s. **n 2배마다 4배 = O(n²)** 가 눈으로 보인다.
  같은 입력에서 스택 방식은 정확히 2배씩만 늘었다.

- **배운 것 2 — 스택을 쓰는 이유는 "새로 생기는 변화가 맨 위에서만 일어나기" 때문이다.**
  새 재료를 얹었을 때 새로 완성될 수 있는 햄버거는 맨 위 4개뿐이다. 그 아래는 하나도 안 변했다.
  그래서 다시 훑을 필요가 없고, 빼내면 그 아래가 새 맨 위가 되어 **"위아래가 맞붙는" 처리도 저절로 된다.**

- **배운 것 3 — `std::stack` 이 아니라 `vector` 를 쓴다.**
  `std::stack` 은 **`top()` 하나만** 보여준다. 이 문제는 위에서 4개를 봐야 하므로 못 쓴다.
  코테에서 스택은 대개 `vector` + `push_back`/`pop_back`/`back()` 이면 충분하고, 안쪽도 들여다볼 수 있다.

- **배운 것 4 — 값으로 받은 매개변수는 버퍼로 재사용할 수 있다.** (72번과 같은 원리)
  `top` 은 1씩 늘고 4씩 줄므로 **"쓰는 자리(top) <= 읽는 자리"** 가 항상 성립한다.
  최악이어도 방금 읽은 자기 자리에 쓸 뿐이고 그 값은 이미 복사돼 있다. 안 읽은 값을 덮을 수 없다.

- **배운 것 5 — `reserve` 는 항상 이득이 아니다.** 실측(100만 개, 전부 햄버거):

  | 구현 | 전부 햄버거 | 랜덤 | 추가 공간 |
  | --- | ---: | ---: | ---: |
  | 별도 vector 스택 | 0.60 ms | 3.83 ms | O(n) |
  | + `reserve` + `resize` | **0.94 ms** | 3.44 ms | O(n) |
  | **입력 재사용 + `top`** | **0.45 ms** | **3.17 ms** | **O(1)** |

  `reserve` 를 넣은 쪽이 **더 느리다.** 이 입력은 스택이 4를 넘지 않는데 400만 바이트를 미리 잡기 때문.
  85번에서는 같은 `reserve` 가 2배 이득이었다. → **기법이 아니라 상황이 성능을 정한다. 재봐야 한다.**

### 23 문자열 나누기 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : 같은 개수·다른 개수 두 카운터를 들고, 같아지면 덩어리 확정 + 카운터 리셋
- ② 실제 정답 방법 : **차이 카운터 하나**를 들고, `cnt == 0` 인 순간에 기준 문자를 새로 정하며 **그때 센다**
- ③ 사용한 자료구조 : 없음. 정수 카운터 하나 + `char` 하나. `O(n)` 시간 / `O(1)` 공간
- ④ 틀린 이유 : **세 번.**
  - 기준 문자 `x` 를 루프 밖에서 한 번만 정함 → 덩어리가 끝나도 기준이 그대로라 이후가 전부 어긋남
  - 기준을 정하는 글자를 `continue` 로 건너뜀 → **기준 문자 자신이 아무 쪽에도 안 세어짐** (`same` 이 끝까지 0)
  - **규칙 3(끝까지 균형이 안 맞으면 그것도 한 덩어리)을 빠뜨림.**
    `"a"` → 0, `"abc"` → 1, 주어진 예제 `"abracadabra"` 도 5. 랜덤 200,000건 중 **143,185건(72%) 오답**

- **배운 것 1 — 두 수의 "같음"은 차이 카운터 하나로 줄어든다.**
  `same == diff` ⟺ `same - diff == 0`. 카운터 하나와 분기 하나가 동시에 사라진다.
  같은 패턴: **괄호 짝 맞추기**(여는 +1 / 닫는 -1), **보이어-무어 다수결**. LV2 `올바른 괄호` 가 이것이다.

- **배운 것 2 — 세는 시점을 "끝"이 아니라 "시작"으로 옮기면 경계 처리가 사라진다.**
  `cnt == 0` = "새 덩어리가 시작된다"이므로 그 자리에서 세면,
  끝까지 균형이 안 맞는 마지막 덩어리도 **이미 세어져 있다** → 루프 뒤 `if` 가 필요 없다.
  이날 72번에서 `if (s.back() == ' ')` 를 없앤 것과 같은 종류. **경계 처리는 잘 짜는 것보다 없애는 게 낫다.**
  실측(200,000자): 카운터 2개 + leftover 0.0040s → 차이 카운터 1개 0.0032s.

- **배운 것 3 — 6월에 이미 알아냈던 것을 다시 헤맸다.**
  `solution2` 주석에 위 두 가지가 그대로 적혀 있었는데, 다시 풀 때 `solution1` 쪽 구조로 시작해 세 번 틀렸다.
  81번에 이어 두 번째다. **문제를 시작하기 전에 그 문제의 기존 기록부터 읽을 것.**

### 72 이상한 문자 만들기 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : `split(" ")` 한 뒤 **단어 인덱스**로 단어 전체를 대/소문자 변환
- ② 실제 정답 방법 : 단어로 나눈 뒤 **그 단어 안의 글자 인덱스**로 글자마다 변환. 이중 루프
- ③ 사용한 자료구조 : `vector<string>` + 수동 join. `O(L)` 시간 / `O(L)` 공간
- ④ 틀린 이유 : **네 번.** 인덱스가 무엇의 인덱스인지 계속 어긋났다.
  - `value == " "` 로 공백을 잡으려 함 → **`split` 은 구분자를 버린다.** 공백은 결과에 들어오지 않는다
  - 단어 인덱스로 단어 전체를 변환 → `'TRYhelloWORLD'`. 문제가 말하는 인덱스는 **단어 안 글자 번호**
  - `" ".join(문자열)` → 리스트가 아니라 문자열을 넘겨 **글자마다** 공백이 들어감 (`'T r Y ...'`)
  - C++ `getline` 이 끝 공백 조각을 버림 → `"abc "` 가 `"AbC"`. 8케이스 중 3개 오답

- **배운 것 1 — Python `split(" ")` 과 C++ `getline(ss, w, ' ')` 은 끝에서 다르다.**
  문자열이 구분자로 끝나면 Python은 마지막 빈 조각을 만들고 **C++은 만들지 않는다.**
  차이는 **항상 정확히 하나**라, 끝이 공백이면 하나를 되돌려 놓으면 된다.
  실측: 이 보정 전 8/9 케이스 중 3개 오답 → 보정 후 랜덤 300,000건 불일치 0.

- **배운 것 2 — 나누지 않는 편이 짧고 빠르다. 최종적으로 이 방식으로 교체했다.**
  글자를 한 번 훑으며 카운터를 들고, 공백을 만나면 카운터를 0으로 리셋하면 끝난다.
  `split`·`join`·빈 조각·이중 루프가 전부 사라지고 **끝 공백 함정도 생기지 않는다.**

  세 구현을 랜덤 300,000건으로 동치 확인 후 실측(C++):

  | 구현 | 200자(제한 최대) | 200,000자 |
  | --- | ---: | ---: |
  | A  split + join + 끝 공백 보정 | 0.829 µs | 990 µs |
  | B  한 번 순회 + 새 문자열 | 0.244 µs | 222 µs |
  | **C  한 번 순회 + 제자리 수정** | **0.192 µs** | **184 µs** |

  C가 A보다 **4.3배 빠르고 본문은 52줄 → 29줄**. → **"문자열을 쪼갠다"가 항상 첫 선택은 아니다.
  상태를 하나 들고 한 번 훑는 쪽을 먼저 검토할 것.**

- **배운 것 4 — 매개변수가 값 복사면 그 복사본을 제자리에서 고쳐 반환하면 된다.**
  `string solution(string s)` 의 `s` 는 이미 호출자 값의 복사본이다. 새 문자열을 또 만들 이유가 없다.
  `for (char& ch : s)` 처럼 **참조**로 받아야 대입이 반영된다. `char ch` 는 사본이라 원본이 안 바뀐다.
  (단, 프로그래머스 스켈레톤 시그니처는 그대로 둔다는 원칙은 유지 — 여기서는 시그니처를 고치지 않았다.)

- **배운 것 3 — 틀린 변수명이 틀린 코드를 유도한다.**
  `for words in arr_s` (실제로는 단어 하나), `for index, word in ...` (실제로는 글자 하나).
  이름이 반대라서 `word.upper()` 로 단어 전체를 바꾸는 실수가 자연스러워 보였다.

### 81 문자열 내 p와 y의 개수 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : p·y가 하나도 없으면 먼저 true를 반환하고, 아니면 개수를 세어 비교
- ② 실제 정답 방법 : 조기 종료를 **빼고**, 한 번의 순회로 둘을 세어 `return p_size == y_size`
- ③ 사용한 자료구조 : 없음. 카운터 두 개. `O(L)` 시간 / `O(1)` 공간
  (Python 풀이는 `lower()`가 새 문자열을 만들어 공간이 `O(L)`, 순회도 3번이었다)
- ④ 틀린 이유 : **조기 종료 한 줄 때문에 랜덤 20만 건 중 37,527건(19%)이 오답.**
  아래 두 줄에서는 대소문자를 둘 다 셌는데, 조기 종료에서는 **소문자만** 봤다.
  `"PP"` → `"p" not in s` 가 참이라 조기 종료로 `True`를 반환. 정답은 `False`.

- **배운 것 — 불필요한 방어 분기가 버그를 만든다.**
  `p_size == y_size` 는 "둘 다 0개"까지 이미 처리한다(`0 == 0`). 그 분기는 **처음부터 필요가 없었고**,
  없었다면 버그도 없었다. **조건 분기는 공짜가 아니다 — 하나 늘 때마다 틀릴 자리가 하나 는다.**
  6월에 같은 파일 주석에 "불필요한 if 분기를 만들지 않는 게 더 안전하고 읽기 쉽다"고 직접 적어놓고
  다시 풀 때 그대로 어겼다. **기록해둔 것을 다시 읽는 것까지가 복습이다.**

- **C++ 문법 — `'p'`(char)와 `"p"`(string)는 다른 타입.**
  `i == "p"` 는 `error: ISO C++ forbids comparison between pointer and integer`.
  `"p"` 는 `{'p','\0'}` 배열의 **주소**이고 `char` 는 숫자값이라 비교가 성립하지 않는다.
  Python에서는 둘이 완전히 같아서 아무거나 써도 됐던 부분이다. 79번에서 적어둔 항목에 또 걸렸다.

### 79 문자열 다루기 기본 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : 루프를 돌며 길이와 숫자 여부를 함께 검사
- ② 실제 정답 방법 : **길이 검사를 루프 밖으로 빼고**, 루프에서는 숫자 여부만 본다 (early return)
- ③ 사용한 자료구조 : 없음. `O(n)` 시간 / `O(1)` 공간
- ④ 틀린 이유 : 정답이긴 했으나 **길이 검사가 루프 안에 있었다.**
  루프와 무관한 조건이라 글자 수만큼 같은 판정을 반복하고, 더 중요하게는
  **빈 문자열이면 루프가 0번 돌아 검사가 아예 실행되지 않고 `True` 가 나온다** (실측 확인).
  이 문제는 제한이 "길이 1 이상"이라 안 터졌다 — **제한이 지켜준 것이지 코드가 옳아서가 아니다.**

- **배운 것 1 — early return.** "조건을 만족하지 않는 것을 앞에서 걸러내고, 통과한 것만 남긴다."
  플래그 변수(`answer = True`)와 `break` 가 사라져 흐름이 한눈에 읽힌다.
  조건이 5~6개 붙는 시뮬레이션 문제에서는 이 모양이 아니면 감당이 안 된다.

- **배운 것 2 — 루프 안에 루프 불변 조건을 두지 않는다.**
  루프를 돌며 값이 변하지 않는 검사는 밖으로 뺀다. 성능보다 **경계 케이스에서 조용히 틀리는 것**이 더 큰 문제다.

- **배운 것 3 — C++에는 문자열 전체를 검사하는 메서드가 없다.**
  Python `s.isdecimal()` 한 줄이 C++에서는 루프가 된다. 그래서 `all(...)` 을 쓰는 Python 풀이가
  C++로 더 자연스럽게 옮겨진다. `isdigit` 대신 `c >= '0' && c <= '9'` 를 쓰면 헤더도 필요 없다.

### 46 두 개 뽑아서 더하기 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : 이중 루프로 모든 쌍의 합을 set에 넣고 정렬해서 반환
- ② 실제 정답 방법 : 같음. C++에서는 `set` 자체가 정렬을 유지하므로 정렬 단계가 사라진다
- ③ 사용한 자료구조 : `set<int>` (중복 제거 + 자동 정렬을 한 번에). `O(n² log n)`, n ≤ 100이라 여유
- ④ 틀린 이유 : **세 번.** 전부 "Python 습관을 C++에 그대로 옮겨서".
  - `for j in range(i, ...)` — 6월 커밋본은 `i+1` 이었는데 다시 쓰면서 `+1` 을 빠뜨림.
    같은 인덱스를 두 번 뽑아 `[2,1,3,4,1]` 에 8(4+4)이 섞임. **문제 조건은 "서로 다른 인덱스"**
  - `tmp_result.add(...)` — C++ `set` 의 삽입은 `insert`. `add` 는 존재하지 않는 이름
  - `set<int>` 를 `vector<int>` 반환 자리에 그대로 돌려줌 — C++은 자동 변환하지 않는다

- **배운 것 — 이름이 같아도 성질이 다르다.**
  Python `set` 은 순서가 없고, **C++ `set` 은 항상 오름차순**이다 (내부가 균형 트리).
  Python `set` 에 성질이 대응하는 건 `unordered_set` 쪽이다.
  이 문제처럼 "중복 제거 + 오름차순"을 동시에 요구하면 C++ `set` 하나로 두 요구가 다 풀린다.

- **뒤의 두 실수는 컴파일러가 100% 잡아줬다.** Python이었다면 `set` 을 그대로 return해도
  에러 없이 실행되고 채점에서 조용히 틀렸을 것이다. 18번의 `[cards1_index] == g` 오타와 정확히 반대 상황.

### 85 같은 숫자는 싫어 — C++ 재구현 (2026-09-08)
- ① 처음 떠올린 방법 : 인접 비교로 `arr[i] != arr[i+1]` 일 때 `arr[i]` 를 남김
- ② 실제 정답 방법 : 같음. 다만 **마지막 원소는 루프가 판정할 수 없어** 루프 밖에서 무조건 추가
- ③ 사용한 자료구조 : `vector` 하나. `O(n)` 시간 / `O(n)` 공간 — **하한이라 더 줄일 수 없다**
- ④ 틀린 이유 : 맨 끝 원소가 빠졌다. 각 원소를 "다음 원소와 비교"해 판정하므로
  마지막 원소는 비교 상대가 없어 구조적으로 건너뛰어진다. 동시에 뒤가 없으니 연속 중복일 수도 없어 **항상 포함**된다.

- **배운 것 1 — "더 최적화할 수 있나"는 감이 아니라 하한과 비교해서 답한다.**
  입력을 전부 읽어야 하면 `O(n)` 이 하한이고, `O(n)` 풀이가 나왔으면 거기서 끝이다.
  이 판단이 서면 이미 최적인 코드를 붙잡고 시간을 태우지 않는다.
  실측: Python 0.0200s / C++ 0.0019s (원소 100만). `itertools.groupby` 는 0.0283s로 **직접 루프보다 느렸다** —
  파이썬에서 "짧은 코드 = 빠른 코드"가 아니다.

- **배운 것 2 — `vector::reserve`.** `push_back` 은 capacity가 차면 더 큰 메모리를 새로 잡고
  기존 원소를 **전부 복사**한다. capacity는 `1→2→4→8→16→32` 로 배씩 늘어나 전체는 분할상환 `O(n)` 이지만,
  상한을 미리 알면 그 복사를 통째로 없앨 수 있다.
  실측(원소 100만): 재할당 6회 → **0회**, 0.0019s → **0.0010s (약 2배)**.
  **`reserve`는 size를 바꾸지 않는다** — `reserve(5)` 후에도 `size()==0` 이라 `v[0]` 접근은 범위 밖이다.
  원소를 실제로 만드는 건 `resize`. 이 둘을 헷갈리면 조용히 망가진다.
  시그니처를 건드리는 `const vector<int>&` 는 이득이 미미하므로 **스켈레톤 그대로 둔다**(기존 원칙 유지).

  **`reserve` 판단 기준 (실측 3건):**

  | 문제 | 잡은 만큼 채우나 | 결과 |
  | --- | --- | --- |
  | 85 같은 숫자는 싫어 | 결과가 입력 길이에 가까움 → **채운다** | **2배 이득** |
  | 58 직사각형 별찍기 | 최종 길이가 정확히 계산됨 → **채운다** | **2.4배 이득** (줄 단위 처리와 합쳐 75배) |
  | 28 햄버거 만들기 | 스택이 4를 안 넘음 → **거의 안 채운다** | **손해** (0.60ms → 0.94ms) |

  → **`reserve` 는 "잡은 만큼 실제로 채울 때"만 이득이다.** 기법이 아니라 상황이 성능을 정한다.

### 18 카드 뭉치 (2026-09-07)
- ① 처음 떠올린 방법 : 각 뭉치의 원소가 `goal`에서 갖는 인덱스가 오름차순이면 통과
- ② 실제 정답 방법 : `goal`을 한 번 훑으며 단어마다 두 뭉치의 **맨 위**와만 비교, 맞으면 그 뭉치 포인터를 전진
- ③ 사용한 자료구조 : 없음. 뭉치마다 정수 포인터 하나씩 (O(len(goal)) 시간, O(1) 공간)
- ④ 틀린 이유 : **네 번 틀림.** 전부 "제한사항을 안 읽어서" 하나로 수렴한다.
  - `goal`에 없는 카드를 건너뛰어도 된다고 가정 → 카드는 버릴 수 없으므로 쓰는 카드는 뭉치의 **prefix**여야 함
    (`cards1=["a","b"], cards2=["c","d"], goal=["b","c"]` 에서 Yes를 내지만 정답은 No)
  - `for index, card in cards1:` 의 `index`가 `goal`이 아니라 `cards1`의 인덱스라 검사가 항상 참
  - `while goal_index > time:` 부등호 반대 → 루프에 한 번도 진입 못 하고 전부 "No". **세 번 지적받고서야 실행해봄**
  - `[cards1_index] == g` 오타. 리스트 vs 문자열 비교라 파이썬은 조용히 False. C++이었으면 컴파일 에러

- **배운 것 — 그리디가 성립한 이유는 제한사항 한 줄이다.**
  "cards1과 cards2에는 서로 다른 단어만 존재합니다" → 한 단어를 낼 수 있는 뭉치가 많아야 하나 → **갈림길이 없다**.
  이 제한을 풀면 (`cards1=["x","a"], cards2=["x","b"], goal=["x","b"]`) 그리디는 No, 정답은 Yes로 **즉시 깨진다**.
  그때는 되돌아가는 백트래킹이 필요하고, `(i, j)`(각 뭉치를 몇 장 썼나)를 캐싱하면 DP가 된다.
  실측(뭉치 12장씩, goal 24단어, 답 No): 백트래킹 7,696,443회 0.47s → `@lru_cache` 한 줄로 **168회 0.000087s**.
  상태 수가 `13 × 13 = 169`뿐이라 그 이상 계산할 이유가 없다.
  → **완전탐색 ⊃ DP(중복 상태 제거) ⊃ 그리디(선택지가 하나뿐일 때)** 는 별개 기술이 아니라 같은 탐색의 축약 단계다.

- C++ : `int cards1_index` 와 `cards1.size()`(unsigned) 직접 비교 → `-Wsign-compare`.
  `int cards1_size = cards1.size();` 로 받아두고 비교 (NOTES 아래 "부호 비교" 항목 그대로).
  `and` 는 C++에서도 합법이지만 관례는 `&&`.

### 34 신고 결과 받기 (2026-08-20, 재풀이)
- ① 처음 떠올린 방법 : 신고 관계를 정방향·역방향 dict 두 개로 저장하고 교집합
- ② 실제 정답 방법 : 역방향 dict 하나만 만들고, 정지 확정 순간 신고자들의 메일 수를 즉시 +1
- ③ 사용한 자료구조 : dict + set (중복 신고를 set이 자동으로 1회 처리)
- ④ 틀린 이유 : 틀리지는 않았으나 dict 2개는 불필요. 방향을 뒤집으니 메모리 절반, 속도 2배 (0.0805s → 0.0442s)

### 53 완주하지 못한 선수 (2026-08-21)
- ① 처음 떠올린 방법 : `set(participant) ^ set(completion)`
- ② 실제 정답 방법 : 이름별 인원을 dict로 세고, `completion`을 돌면서 하나씩 깎기
- ③ 사용한 자료구조 : dict (`.get(name, 0) + 1` 로 한 바퀴에 집계)
- ④ 틀린 이유 : **두 번 틀림.**
  - set이 중복(동명이인) 정보를 버려서 예제 3에서 빈 집합 → `KeyError`
  - `participant.count(p)` 로 세다가 O(N²) → 10만 명 기준 **추정 124초**. dict 집계로 0.015초
  - 참가자 표를 돌며 "완주자 명단에 있나?" 로 깎으면 같은 이름을 한 번만 깎아 오답 (랜덤 5000회 중 959건 불일치)

### 88 폰켓몬 (2026-08-21)
- ① 처음 떠올린 방법 : 바로 맞힘
- ② 실제 정답 방법 : `min(가져갈 수 있는 마리 수, 종류 수)`
- ③ 사용한 자료구조 : set (종류만 필요하고 각 종류가 몇 마리인지는 답에 무관)
- ④ 틀린 이유 : 없음. 53번과 정확히 반대 — 같은 `set`이 거기선 오답, 여기선 정답

### 52 K번째수 (2026-08-21)
- ① 처음 떠올린 방법 : 바로 맞힘
- ② 실제 정답 방법 : `sorted(array[i-1:j])[k-1]`
- ③ 사용한 자료구조 : list 슬라이싱 + `sorted()`
- ④ 틀린 이유 : 없음. 다만 `j`만 `-1`을 안 하는 이유(슬라이스가 끝을 포함하지 않음)를 주석에 남겨둘 것

---

## C++ 재구현 — Python → C++ 대응 (2026-08-22~27, LV1 8문제)

Python으로 이미 푼 문제를 C++로 옮기며 얻은 것. **알고리즘이 아니라 언어 차이만** 정리한다.

### 문법·타입

| Python | C++ | 처음 걸린 곳 |
| --- | --- | --- |
| `len(x)` | `x.size()` | 86 가운데 글자 |
| `x[a:b]` (문자열) | `x.substr(a, 개수)` — **끝 위치가 아니라 개수** | 86 |
| `x[a:b]` (리스트) | `vector<T> v(x.begin()+a, x.begin()+b)` — 끝 제외는 동일 | 52 K번째수 |
| `sorted(x)` | **없음.** `sort(x.begin(), x.end())` 는 `void` (제자리 정렬) | 52 |
| `s.split()` | `stringstream ss(s); ss >> a >> b;` | 34 신고 결과 |
| `a, b = ...` | 다중 대입 없음. 각각 선언 후 대입 | 34 |
| `d.get(k, 0) + 1` | `d[k] += 1` — 없는 키는 자동으로 0에서 시작 | 53 완주하지 못한 선수 |
| `d.values()` | 없음. `for (const auto& [k, v] : d)` 로 받고 키를 무시 | 34 |
| `for k, v in d.items()` | `for (const auto& [k, v] : d)` (C++17 구조 분해) | 53 |
| `set(nums)` | `unordered_set<int> s(nums.begin(), nums.end());` | 88 폰켓몬 |
| `list.append(x)` | `v.push_back(x)` | 52 |
| `A if c else B` | `c ? A : B` — **조건이 앞** | 66 짝수와 홀수 |
| `for c in s` (c는 문자열) | `for (char c : s)` — **string을 순회하면 `char`** 가 나온다 | 79 문자열 다루기 기본 |
| `'a'` 와 `"a"` 가 같음 | `'a'` 는 `char`, `"a"` 는 `string`. **다른 타입** | 79 |
| `c in "0123456789"` | `c >= '0' && c <= '9'` — char는 숫자값이고 '0'~'9'가 연속 | 79 |
| `s.count("p")` | `count(s.begin(), s.end(), 'p')` — `<algorithm>`. **여러 글자짜리는 표준 함수 없음** | 81 문자열 내 p와 y의 개수 |
| `"*" * n` | **`string(n, '*')`** — 문자 n개짜리 문자열을 한 번에 만든다 | 58 직사각형 별찍기 |
| `"수박" * n` | **`string(n, '수')` 는 불가.** 한글은 `char` 에 안 들어간다 → `answer += "수박";` | 76 수박수박수박수박수박수? |
| `s += str(d) * cnt` | **`s.append(cnt, char('0'+d))`** — 문자 cnt개를 한 번에 붙인다 | 32 숫자 짝꿍 |
| `int(c)` (문자→숫자) | **`c - '0'`** / 역방향은 `char('0' + d)`. 문자 코드가 '0'~'9' 연속이라 성립 | 32 |
| `[0]*10` | `vector<int> v(10, 0)` 또는 **`int v[10] = {0};`** (크기 고정이면 배열이 낫다) | 32 |
| `[[0]*C for _ in range(R)]` | **`vector<vector<int>> v(R, vector<int>(C, 0));`** — Python 의 `[[0]*C]*R` 처럼 행을 공유하는 함정이 없다 | LV2 05 행렬의 곱셈 |
| `-(-a // b)` (올림) | **`(a + b - 1) / b`** — 둘 다 정수만 쓰는 올림 나눗셈 관용구 | LV2 34 기능개발 |
| `deque` / `popleft()` | `#include <deque>`, `d.front()` 로 **보고** `d.pop_front()` 로 **버린다**. **`pop` 은 값을 돌려주지 않는다** | LV2 35 프로세스 |
| `d.rotate(-1)` | `auto f = d.front(); d.pop_front(); d.push_back(f);` | LV2 35 |
| `heapq` (기본 **최소** 힙) | `priority_queue<T>` 는 기본 **최대** 힙. 최소 힙은 **`priority_queue<T, vector<T>, greater<T>>`** | LV2 43 더 맵게 |
| `heapq.heapify(v)` | 범위 생성자 `priority_queue<...> pq(v.begin(), v.end());` — 하나씩 push 보다 빠르다 | LV2 43 |
| `heappop(v)` | `pq.top()` 으로 **보고** `pq.pop()` 으로 **버린다** (queue 와 동일) | LV2 43 |
| (타입 신경 안 씀) | **`auto x = pq.top();`** 로 받으면 컨테이너 타입이 바뀌어도 따라온다. **`auto&` 는 금지** — `pop()` 뒤 죽은 자리를 가리킨다 | LV2 43 |
| `max(x[0] for x in d)` | 컨테이너를 직접 훑어 `max_priority = max(max_priority, p.first);` — 임시 vector 를 만들지 않는다 | LV2 35 |
| `sorted(v, key=lambda x: k(x))` | **`sort(v.begin(), v.end(), [](const T& a, const T& b){ return ...; })`** — key(무엇으로)가 아니라 **순서(누가 먼저인가)** 를 답한다 | 82 문자열 내 마음대로 정렬하기 |
| `key=(x[n], x)` 다중 기준 | 비교 함수에 `if (a[n] != b[n]) return a[n] < b[n]; return a < b;` — **1순위로 판가름 나면 거기서 끝** | 82 |
| (해당 없음) | 비교 함수는 **같으면 반드시 `false`**. `<=` 를 쓰면 모순이 되어 런타임에 터진다 | 82 |
| `len("수박")` = 2 | **`"수박".size()` = 6.** C++ `size()` 는 글자 수가 아니라 **바이트 수** (한글 1글자 = UTF-8 3바이트) | 76 |
| `print(x)` | `cout << x << '\n';` — **`endl` 은 개행 + flush**. 반복 출력에서는 `'\n'` 이 관례 | 58 |
| `s.lower()` / `s.upper()` | **없음.** 글자마다 비교하거나 `tolower(c)` (`<cctype>`) | 81 |
| `enumerate(x)` | **없음.** 인덱스 루프를 쓰거나 카운터를 직접 증가시킨다 | 72 이상한 문자 만들기 |
| `s.split(" ")` | `stringstream ss(s); while (getline(ss, w, ' '))` — 헤더는 **`<sstream>`** | 72 |
| `s.split()` (인자 없음) | `ss >> w` — 연속 공백을 합친다. **구분자 명시가 필요하면 쓰면 안 된다** | 72 |
| `" ".join(v)` | **없음.** 루프에서 `if (i > 0) answer += ' ';` 후 이어붙인다 | 72 |
| `toupper` / `tolower` | **소문자 이름.** `toUpper` 같은 카멜케이스는 C 표준에 없다. `int` 반환이라 `char` 로 받는다 | 72 |
| (문자열은 불변이라 불가) | `for (char& ch : s)` — **참조로 받으면 제자리 수정.** `char ch` 는 사본이라 원본이 안 바뀐다 | 72 |
| `x = ""` 로 문자 담기 | 글자 하나는 **`char`**. `string base; base = ch;` 는 되지만 **`base == ch` 는 컴파일 에러** | 23 문자열 나누기 |
| `lst[-1]` | **음수 인덱스 없음.** `v.back()` 또는 `int n = v.size();` 후 `v[n-1]` | 28 햄버거 만들기 |
| `lst.index(x)` | `auto it = find(v.begin(), v.end(), x);` 후 **`distance(v.begin(), it)`** (`<algorithm>`). `find` 는 **이터레이터**를 돌려준다 | 78 서울에서 김서방 찾기 |
| `str(n)` / f-string | **`to_string(n)`.** C++에는 문자열 포매팅이 없어 `+` 로 이어붙인다 | 78 |
| `del lst[-4:]` | `v.resize(n - 4)` (또는 `pop_back()` 4번). `resize` 는 **개수를 그 수로 맞춘다** | 28 |
| `lst[-4:] == [1,2,3,1]` | 슬라이스 비교 없음. **네 인덱스를 직접 비교** | 28 |
| `True` / `False` | `true` / `false` — **소문자.** 대문자는 존재하지 않는 이름 | 79 |
| `if cond:` + 들여쓰기 | `if (cond) { ... }` — **괄호 필수, 콜론 없음, 블록은 중괄호** | 79 |
| (제어 수단 없음) | **`v.reserve(n)`** — 자리만 미리 확보. C++에만 있는 손잡이 | 85 같은 숫자는 싫어 |
| `s.add(x)` | `s.insert(x)` — **`add` 라는 이름은 C++에 없다** | 46 두 개 뽑아서 더하기 |
| `sorted(set)` | `set<T>` 는 **넣는 순간 정렬 유지** → `vector<int> v(s.begin(), s.end())` 로 끝 | 46 |
| `set` (순서 없음) | `unordered_set<T>` ← 이쪽이 Python `set` 에 대응. C++ `set` 은 **정렬된 set** | 46 |
| `[0] * n` | `v.resize(n)` 또는 `vector<int> v(n, 0)` — **`reserve`가 아니다** | 85 |

### 조용히 틀리는 것들 — 이게 진짜 위험

| 함정 | 증상 | 대응 |
| --- | --- | --- |
| **오버플로** | `int`로 누적하다 21억 넘으면 **음수**가 됨. 에러도 경고도 없음 | 누적·곱셈 결과가 21억 넘으면 `long long` |
| **계산 타입** | `long long`에 담아도 `int * int`는 `int`로 계산돼 이미 망가짐 | **곱하기 전에** 한쪽을 `long long`으로 |
| ↑ 실측 (LV1 59) | `x=10,000,000, n=1,000` 에서 **1000개 중 786개(79%) 오답.** `i=214` 에서 21.5억이 `-2,144,967,296` 으로 뒤집힘. **주어진 예제는 값이 작아 그대로 통과한다** | 제한 숫자를 곱해 **21억을 넘으면 `long long`** — 복잡도뿐 아니라 타입도 제한에서 나온다 |
| **`%` 음수** | Python `-3 % 2 == 1`, C++ `-3 % 2 == -1` | 홀짝 판별은 `== 0` / `!= 0` 로 |
| **`/` 음수** | Python `-3 // 2 == -2`(내림), C++ `-3 / 2 == -1`(0쪽 버림) | 음수 나눗셈 나오면 직접 확인 |
| **`//`** | C++에서는 **주석**. `x // 2` 쓰면 뒷부분이 통째로 사라짐 | 정수 나눗셈은 `/` 하나로 충분 |
| **부호 비교** | `size()`(unsigned) `>= k`(int) 에서 k가 음수면 거대한 수로 변환 → `2 >= -1`이 **거짓** | `int n = x.size();` 로 받아두고 비교 |
| **return 누락** | Python은 `None`, C++은 **미정의 동작**. 테스트가 통과해도 운일 뿐 | 마지막에 `throw` 또는 명시적 return |
| ↑ 컴파일러가 알려준다 (LV1 35) | `warning: control reaches end of non-void function [-Wreturn-type]`. **로직상 도달 못 해도 컴파일러는 증명할 수 없다** | 기본값을 둔다면 **답이 될 수 없는 값**으로. `return n-1;` 처럼 답이 될 수 있는 값은 버그를 가린다 |
| ↑ 그릇이 아니라 **계산 위치** (LV2 43) | `priority_queue<long long>` 에 담아도 `int a = pq.top();` 로 꺼내 `int` 로 계산하면 **거기서 이미 넘친다** | **꺼내는 변수와 계산식까지** 전부 큰 타입으로. `auto` 로 받으면 자동으로 따라온다 |
| **`unordered_map` 순서** | Python dict는 삽입 순서 보장, C++ `unordered_map`은 **순서 없음** | 결과는 원본 목록 순서로 다시 뽑기 |

### 이름 충돌 — `using namespace std;` 의 대가

`std` 의 이름을 지역 변수·함수 이름으로 쓰면 부딪힌다. 실제로 겪은 것:

```
string ref(string s) { ... }
→ error: conversion from 'std::reference_wrapper<...>' to non-scalar type 'std::string'
```

피할 이름: `ref` `count` `max` `min` `size` `data` `begin` `end` `next` `prev` `swap` `distance` `find` `sort` **`stack`** `queue` `array`
→ `cnt`, `expected_of` 처럼 접두·접미를 붙인다.

### 환경·제출

- `sort`는 `<algorithm>`, `runtime_error`는 `<stdexcept>`. **프로그래머스 스켈레톤에는 `<string>`, `<vector>`만 있다.**
  로컬은 통과하는데 프로그래머스에서 `undeclared identifier` 가 뜨면 **거의 항상 include 누락**.
- 프로그래머스는 **clang**, 로컬은 **g++**. 같은 에러를 다르게 표현한다.
- 제출할 때 `#include "../test_runner.h"` 와 `int main()` 은 **빼고** `solution` 함수만 올린다.
- 오타는 C++이 **컴파일 시점에 100% 잡아준다** (`did you mean ...?` 제안까지). 긴 변수명은 타이핑하지 말고 복사.

### 성능 — C++이 항상 빠르지는 않다 (실측)

53번 완주하지 못한 선수, 참가자 100,000명 기준:

| 구현 | 시간 |
| --- | ---: |
| Python | **0.0091s** |
| C++ (프로그래머스 스켈레톤 그대로, 값 복사) | 0.0099s |
| C++ (`const vector<string>&` 참조) | **0.0076s** |

- `vector`를 값으로 받으면 원소를 **전부 복사**한다 (10만 개면 10만 번).
- Python `dict`/`str` 해싱은 C로 짜인 최적화 코드라 해시 위주 문제에서는 C++와 대등하다.
- C++이 크게 이기는 건 **반복 자체가 무거울 때** (83번의 2천만 번 덧셈 등).
- 실전에서는 **스켈레톤 그대로 쓴다.** 시그니처를 고치다 실수하는 비용이 더 크다.
